<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tot.dao.TourDao">

    <select id="getTourById" parameterType="String" resultType="TourVO">
        SELECT * FROM TOUR WHERE TOID = #{tourId}
    </select>

</mapper>
